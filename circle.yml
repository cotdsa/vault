general:
  branches:
    only:
      - cotdsa

checkout:
  post:
    - mkdir -p $HOME/.go_workspace/src/github.com/hashicorp
    - ln -s `pwd`/vault $HOME/.go_workspace/src/github.com/hashicorp/

general:
  build_dir: ../.go_workspace/src/github.com/hashicorp/vault

dependencies:
  override:
    - make bootstrap

compile:
  override:
    - XC_OS="linux" XC_OSARCH="linux/386 linux/amd64" make bin

test:
  override:
    - make test
